name: trend-pullback-reclaim-r1

on:
  pull_request:
    paths:
      - '.github/workflows/trend-pullback-reclaim-r1.yml'
      - 'scripts/run_trend_pullback_reclaim_r1.mjs'
      - 'scripts/aggregate_trend_pullback_reclaim_r1.mjs'
      - 'docs/TREND_PULLBACK_RECLAIM_R1.md'
  workflow_dispatch:

concurrency:
  group: trend-pullback-reclaim-r1-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
      - name: Verify frozen protocol
        run: test "$(git rev-parse HEAD:docs/TREND_PULLBACK_RECLAIM_R1.md)" = "cbd0fe94896de6c827dd8baf74b4d090e92313f8"
      - name: Syntax checks
        run: |
          node --check scripts/run_trend_pullback_reclaim_r1.mjs
          node --check scripts/aggregate_trend_pullback_reclaim_r1.mjs

  symbol:
    needs: validate
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        symbol:
          - BTCUSDT
          - ETHUSDT
          - BNBUSDT
          - XRPUSDT
          - ADAUSDT
          - SOLUSDT
          - DOGEUSDT
          - LINKUSDT
          - AVAXUSDT
          - DOTUSDT
          - LTCUSDT
          - ATOMUSDT
          - AAVEUSDT
          - NEARUSDT
          - INJUSDT
          - OPUSDT
          - ARBUSDT
          - APTUSDT
          - SUIUSDT
          - SEIUSDT
          - TAOUSDT
          - ONDOUSDT
          - TONUSDT
          - FETUSDT
          - WIFUSDT
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
      - name: Ensure archive tools
        run: command -v unzip >/dev/null || (sudo apt-get update && sudo apt-get install -y unzip)
      - name: Run frozen pullback matrix
        env:
          TPR_SYMBOL: '${{ matrix.symbol }}'
        run: node scripts/run_trend_pullback_reclaim_r1.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trend-pullback-reclaim-${{ matrix.symbol }}
          path: runtime/trend-pullback-reclaim-r1/${{ matrix.symbol }}.*
          if-no-files-found: error
          retention-days: 30

  aggregate:
    needs: symbol
    if: always() && needs.symbol.result == 'success'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
      - uses: actions/download-artifact@v4
        with:
          pattern: trend-pullback-reclaim-*
          path: trend-pullback-reclaim-results
          merge-multiple: true
      - name: Aggregate under frozen multi-candidate criteria
        run: node scripts/aggregate_trend_pullback_reclaim_r1.mjs trend-pullback-reclaim-results trend-pullback-reclaim-r1-summary.json
      - uses: actions/upload-artifact@v4
        with:
          name: trend-pullback-reclaim-r1-summary
          path: |
            trend-pullback-reclaim-r1-summary.json
            trend-pullback-reclaim-r1-summary.md
          if-no-files-found: error
          retention-days: 90
