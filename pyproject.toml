[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-trading-terminal"
version = "1.0.0"
description = "Research and paper-trading terminal for Smoke HYBRID v2"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["strategy_lab*", "backend*"]

